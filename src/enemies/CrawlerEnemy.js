// CrawlerEnemy.js — basic melee chaser. Mirrors scripts/enemies/CrawlerEnemy.gd.
import { EnemyBase } from './EnemyBase.js';

export class CrawlerEnemy extends EnemyBase {
  // Uses base tickBehavior: chase → attack when in range.
}
